const Util = {
	initAnimationDuration: 1000,
	rotateAnimationDelay: 300,
};

// navbar itself is at z-20
// the vertical nav dropdown is at z-10
// everything else is z-0
// the background image in hero should be at z of -1

export default Util;
